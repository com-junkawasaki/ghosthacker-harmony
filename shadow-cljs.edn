;; shadow-cljs build for the HARMONY browser host adapter (ADR-2607100900
;; follow-up (b)). Same layout as ghosthacker-flow's browser build.
{:deps   {:aliases [:cljs]}
 :builds {:app {:target     :browser
                :output-dir "public"
                :asset-path "."
                :modules    {:main {:init-fn ghosthacker-harmony.web/init}}
                :dev-http   {8294 "public"}}}}
